# Audit Report: workstream-b-finish-progress.md, workstream-c-plan-routines.md
**Last Verified:** 2026-09-04
**Verification Status:** Fully Verified

## Summary Table
| Task / Claim | Claimed Status | Actual Verified Status | Evidence |
| :--- | :--- | :--- | :--- |
| WS-B Task 1: `rank_events.plausibility_reason` column + migration | Done | VERIFIED COMPLETE | `packages/db/src/schema.ts:286`; migration `packages/db/drizzle/0011_petite_betty_ross.sql` present, journal entry idx 11 in `meta/_journal.json` |
| WS-B Task 1: `recomputeRankForExercise` 4th param + `insertRankEvent` threading | Done | VERIFIED COMPLETE | `packages/server/src/services/rankService.ts:96-100,303-308` |
| WS-B Task 1: `computeRankEventsByWeekday` returns `flaggedCount` | Done | VERIFIED COMPLETE | `rankService.ts:367,376-387`; route schema `packages/server/src/routes/rankEvents.ts:15-16`; client type `packages/client/src/services/rankEventsService.ts` |
| WS-B Task 1: new rankService tests pass | Done | VERIFIED COMPLETE | `pnpm vitest run rankService.test.ts` → 28/28 pass; tests present at lines 420, 460 |
| WS-B Task 2: `sessionRankUps` checks `plausibilityReason`, `plausibilityNote` field added | Done | VERIFIED COMPLETE | `packages/client/src/composables/useWorkoutFinish.ts:183-194`; `FinishSequence.vue:18-29` |
| WS-B Task 2: muted badge-ring / discounted row styling | Done | VERIFIED COMPLETE | `FinishSequence.vue:61 (topTierClass filters genuine), 172,175,183, 307 (.badge-ring-muted)` |
| WS-B Task 3: `RankUpCalendar.vue` hasGenuine/flagged dot | Done | VERIFIED COMPLETE | `packages/client/src/components/rank/RankUpCalendar.vue:25,32,45-46,113` |
| WS-B Task 4: LP bar animates prevLp→lp (array rAF loop) | Done | VERIFIED COMPLETE | `FinishSequence.vue:84-108 (animateRankUpBars), 114-116 (watch trigger), 144 (onBeforeUnmount cleanup), 185 (template wiring)` |
| WS-B Task 5: Ranks/Progress trust legibility | Verify-only, confirmed already done | VERIFIED COMPLETE | `RankProgress.vue:72(trustLabel),101(≈ marker),109(rp-trust)`; `RanksPage.vue:74-78 (InfoToggle)` |
| WS-B Task 6: Overall Rank placement (3 sites) | Verify-only, confirmed already done | VERIFIED COMPLETE | `RanksPage.vue:63 (TierLadder)`, `OverviewPage.vue:289 (StatTile reward)`, `WorkoutPage.vue:140 (overallRank.current in share tier)` |
| WS-B Task 7: Streak/XP mechanics redesign | Deferred — design-only, no implementation in this plan | VERIFIED COMPLETE (as deferred) | Spec exists at `docs/superpowers/specs/2026-09-04-streak-xp-mechanics-design.md`; see below for cross-check that it is NOT implemented in code |
| Cross-check: streak/XP spec implementation status | Spec "ready for writing-plans"; not yet an implementation plan | VERIFIED — SPEC ONLY, NOT IMPLEMENTED | `packages/shared/src/math/xp.ts:41` still uses `weightKg ?? BODYWEIGHT_NOMINAL_LOAD_KG` (fabricable-weight formula the spec explicitly wants changed) — spec's one-line change NOT applied. `grep -rn "consistencyBonusXp\|varietyBonusXp\|CONSISTENCY_BASE\|VARIETY_PER_MUSCLE" packages/` → zero results. No new `workouts` columns, no Finish Sequence breakdown UI. |
| WS-C Status-check: wizard step-indicator accuracy already fixed | Already fixed, no task needed | VERIFIED COMPLETE | `RoutineWizard.vue:336 (showFastPath), 442-443 (conditional step labels)` |
| WS-C Status-check: mesocycle week-N-of-M + attach/end + 2-16 weeks | Already shipped | VERIFIED COMPLETE | `useMesocycleControls.ts:13-14 (MIN/MAX 2/16)`; `WorkoutPage.vue:458`, `RoutineList.vue:120` (Woche N/M in both active rail and card) |
| WS-C Status-check: routine archive is soft-delete | Already shipped | VERIFIED COMPLETE | `packages/db/src/schema.ts:79 (archivedAt)`; `packages/server/src/routes/routines.ts:93` comment confirms soft-delete DELETE handler |
| WS-C Task 1: server `missingEquipment` on substitution picks | Done | VERIFIED COMPLETE | `routineSuggestionService.ts:92,136,166,187,212`; tests pass (`routineSuggestionService.test.ts` 7/7) |
| WS-C Task 2: client plumbing of `missingEquipment` through service/wizard | Done | VERIFIED COMPLETE | `routineService.ts:63`; `RoutineWizard.vue:68,92` |
| WS-C Task 3: substitution copy names equipment (FastPathStep/ReviewStep) | Done | VERIFIED COMPLETE | `FastPathStep.vue:16,27,47-52,86`; `ReviewStep.vue:12,27,41-46,87` |
| WS-C Task 4: exercise-name truncation (TruncatingLabel) | Done | VERIFIED COMPLETE (superseded by later Foundation primitive, functionally same requirement) | `ExerciseRow.vue:26` now uses `<TruncatingLabel>` component (`packages/client/src/components/ui/TruncatingLabel.vue` exists) rather than the plan's raw-CSS stand-in — a later change adopted the real primitive the plan anticipated; requirement (flex+truncate, no mid-word hyphenation) is met either way |
| WS-C Task 5: extract `RoutineList.vue` out of `WorkoutPage.vue` | Done | VERIFIED COMPLETE | `packages/client/src/components/routine/RoutineList.vue` exists; `WorkoutPage.vue:18,424` renders `<RoutineList v-else-if="!store.isActive" />` |
| WS-C Task 6: drag-to-reorder routine list | Done | VERIFIED COMPLETE | `RoutineList.vue:20,54-73` (useDragReorder wiring, with a documented post-plan fix — "Final-review finding 5a" catch handler on `.reorder()`); `routineStore.ts:103 (reorder action)`; `routineService.ts:89-91 (orderIndex payload)` |
| WS-C Task 7: soften `ExerciseDemo.vue` placeholder | Done | VERIFIED COMPLETE | `ExerciseDemo.vue:119,127 (radial-gradient placeholder, matches ExerciseThumb treatment)` |
| WS-C Task 8: server accepts muscle tags on custom exercise | Done | VERIFIED COMPLETE | `exerciseRepository.ts:21,24-44 (insertCustomExercise + muscleSlugs)`; `routes/exercises.ts:42,51 (schema)`; test file exists, 3/3 pass |
| WS-C Task 9: client add-custom-exercise form | Done | VERIFIED COMPLETE | `AddCustomExerciseForm.vue` exists; `ExercisesPage.vue:13,31,48,53-54` wires trigger/modal (using real `SheetModal` ref/dismiss contract, resolving the plan's flagged open unknown correctly) |
| WS-C Status-check: exercise catalog filters, per-set targets, superset grouping, wizard drag-reorder already implemented | Already shipped, no task | UNVERIFIED-NEEDS MANUAL CHECK | Not directly re-verified line-by-line in this pass (out of scope of the "real gaps" list); no evidence found contradicting the claim, but not independently traced through `ArrangeStep.vue`/`ExerciseList.vue` in this audit |

## Verified Completed Items
* **rank_events plausibility migration (WS-B Task 1)**: Verified via `packages/db/drizzle/0011_petite_betty_ross.sql` and `rankService.ts:96-100,303-308,367-387`. Column, function signature, and weekday-split aggregation all present and covered by 2 new passing tests (28/28 total in the file).
* **Finish Sequence plausibility muting (WS-B Task 2)**: Verified via `useWorkoutFinish.ts:183-194` and `FinishSequence.vue:18-29,61,172-183,307`. `sessionRankUps` now carries `plausibilityNote` and the template renders `.badge-ring-muted`/`.discounted` for flagged rows, `topTierClass` excludes discounted rank-ups from the background wash.
* **RankUpCalendar dot muting (WS-B Task 3)**: Verified via `RankUpCalendar.vue:25,32,45-46,113`. `hasGenuine` correctly computed as `count > flaggedCount`.
* **LP bar animation (WS-B Task 4)**: Verified via `FinishSequence.vue:84-108,114-116,144,185`. `animateRankUpBars` is a real rAF loop reading `prevLp`/`lp` per row, triggered on `activeIndex===0`, cleaned up on unmount.
* **Ranks/Progress trust + Overall Rank placement (WS-B Tasks 5-6)**: Verified via direct read of `RankProgress.vue`, `RanksPage.vue`, `OverviewPage.vue`, `WorkoutPage.vue` — all three render sites and the trust label/marker are real, visible UI, not dead/hidden code.
* **Equipment-substitution naming (WS-C Tasks 1-3)**: Verified via `routineSuggestionService.ts` (server, tested), `routineService.ts`/`RoutineWizard.vue` (client plumbing), and `FastPathStep.vue`/`ReviewStep.vue` (copy). `pnpm vitest run routineSuggestionService.test.ts` → 7/7 pass including both new tests.
* **RoutineList extraction + drag-reorder (WS-C Tasks 5-6)**: Verified via `RoutineList.vue`, `WorkoutPage.vue:18,424`, `routineStore.ts:103`, `routineService.ts:89-91`. Extraction is clean (`WorkoutPage.vue` no longer contains the `.not-started` block inline) and drag-reorder is wired end-to-end with a documented post-plan bugfix.
* **ExerciseDemo placeholder softening (WS-C Task 7)**: Verified via `ExerciseDemo.vue:119,127` — radial-gradient placeholder matching `ExerciseThumb.vue`'s treatment, flat "Kein Bild" box replaced.
* **Custom exercise creation, server + client (WS-C Tasks 8-9)**: Verified via `exerciseRepository.ts`, `routes/exercises.ts`, `AddCustomExerciseForm.vue`, `ExercisesPage.vue`. Server tests pass (3/3); client wiring uses the real `SheetModal` contract, correctly resolving the plan's own flagged unknown.
* **Full server regression suite**: `pnpm vitest run` in `packages/server` → 8 files, 73/73 tests passing, confirming no regressions from either workstream's changes.
* **Client typecheck**: `npx vue-tsc --noEmit` produced zero errors, confirming the client-side type threading (RankUpSummary, SuggestedExercise, suggestionMeta, RankEventsByWeekday) is internally consistent.

## Unresolved & False Positives
None found. Every task marked complete/done in both plan files was independently verified against current source and, where applicable, passing tests. No dead code or unwired features were found among the checked items.

## Unverified (Needs Manual Check)
* **Streak/XP mechanics redesign — runtime/UX verification**: The spec (`docs/superpowers/specs/2026-09-04-streak-xp-mechanics-design.md`) is confirmed to be design-only and NOT implemented in code — `packages/shared/src/math/xp.ts` still contains the pre-redesign formula (`weightKg ?? BODYWEIGHT_NOMINAL_LOAD_KG`, i.e., weight is NOT yet always ignored) and no `consistencyBonusXp`/`varietyBonusXp` columns or constants exist anywhere in the repo. This matches both files' claims (Task 7 deferred; spec "ready for writing-plans", not yet planned/built). No discrepancy — flagged here only because it required cross-referencing three documents plus a repo-wide grep to confirm the negative claim ("not yet implemented") rather than a single file read.
* **WS-C "already implemented, no task needed" claims not directly targeted by a task** (per-set targets/rest overrides/superset grouping in `ArrangeStep.vue`, full catalog filter set in `ExerciseList.vue`, wizard drag-reorder): these were asserted by the plan's own "Status check" section as prior-audit-confirmed and were not part of this pass's active task list, so they were not independently re-traced line-by-line here. Spot checks (mesocycle weeks, step indicator, archive) that were within the "real gaps" cross-reference all held up; no reason to doubt the rest, but they weren't directly read.
