# Audit Report: workplan-v1.md
**Last Verified:** 2026-09-04
**Verification Status:** Discrepancies Found

## Summary Table
| Task / Claim | Claimed Status | Actual Verified Status | Evidence |
| :--- | :--- | :--- | :--- |
| §1.1 195px nav-overflow fix | Fixed (verify only) | VERIFIED COMPLETE | `App.vue:341-359` reflow-audit media query at 380px/300px with explicit "measured 195px" comment |
| §1.2 Silent routine-save failure on fractional reps | Fixed | VERIFIED COMPLETE | `RoutineWizard.vue:350` `Math.max(1, Math.round(s.reps))`, `:361` try, `:366` `toast("Speichern fehlgeschlagen...")` |
| §1.3 NumberStepper digit-concatenation | Fixed | VERIFIED COMPLETE | `NumberStepper.vue:116` `@focus="($event.target as HTMLInputElement).select()"` |
| §1.4 Duplicate finish-screen XP/level display | Fixed | VERIFIED COMPLETE | `App.vue:128-135` `hideTopHud` computed with citing comment; `:169` `v-if="!hideTopHud && ..."` |
| §1.5 Persistent top-HUD during active logging | Fixed (same commit as §1.4) | VERIFIED COMPLETE | Same `hideTopHud` conditional gates `.top-hud`, `App.vue:169` |
| §1.6 Muscle-diagram rendering glitch | Fixed | VERIFIED COMPLETE | `MuscleFigure.vue:126` `aspect-ratio: 200 / 369`, `:140` `object-fit: contain` |
| §1.7 Touch-target floor sweep (44px) | Fixed | VERIFIED COMPLETE | `tokens.css:227` `--touch-target-min: 44px` consolidated token, used at lines 332-333, 377, 404 (btn-close/primary/secondary) |
| §1.8 Segmented-control neutral accent color | Decided/shipped | VERIFIED COMPLETE | `WorkoutRunsSwitcher.vue:61` `background: var(--blue)` fixed regardless of section, with citing comment at :58-60 |
| §1.9a Token field reveal/hide toggle (Profile + AuthGate) | Fixed | VERIFIED COMPLETE | `ProfilePage.vue:155,403,410-413` and `AuthGate.vue:18,65,73-76` both have `tokenVisible` ref + 👁/🙈 toggle |
| §1.9 "Starten" vs "Start" standardization | Not re-verified, status unknown | VERIFIED COMPLETE (best-effort) | No occurrence of unconjugated `"▶ Start"` found in `packages/client/src`; all call sites use "▶ Starten" (`OverviewPage.vue:261`, `RoutineList.vue:138,167`) |
| §1.9 Wizard step-indicator relabel (fast path) | Fixed | VERIFIED COMPLETE | `RoutineWizard.vue:434-443` — step 3 "3 Fertig" only rendered `v-if="!showFastPath"`, with citing comment "Audit fix (workplan-v1 §1.9c)" |
| §1.10 Bottom-anchor primary CTAs | Low-severity batch item, no explicit status marker | UNVERIFIED-NEEDS MANUAL CHECK | Layout/viewport-dependent claim; not practically verifiable via static grep, needs live/visual check |
| §1.10 Bodyweight empty-state copy (names the action) | Decided/implied fix | VERIFIED COMPLETE | `OverviewPage.vue:341-350`, explicit comment "Audit fix (workplan-v1 §1.10b)", copy now reads "Trag dein Körpergewicht in Profil ein..." |
| §1.10 Missing-photo treatment (tiers 2-3 only, full-catalog illustration deferred) | Decided/deferred, tier-3 fallback softened | VERIFIED COMPLETE (tier 3 only) | `ExerciseThumb.vue:39-42` radial-gradient fallback with citing comment; `audit/missing-photo-sourcing-research.md` confirms tier-2 (real photos for the 11/12 gaps) is still research-only, not executed — consistent with doc's own "still open" framing in open question 9 |
| §2 Personal Records ledger | ⏳ **Not started** (explicit "no `/api/prs` route, no client screen" claim) | **FALSE POSITIVE (stale) — actually fully implemented** | `packages/server/src/routes/prs.ts` (registered in `app.ts:19`), `packages/client/src/stores/prStore.ts`, `packages/client/src/services/prService.ts`, `packages/client/src/pages/RecordsPage.vue`, routed at `router.ts:14` (`/records`), linked from `RanksPage.vue:70`. See note below. |
| §3.1 Anti-farming copy reception | Open, needs user check | VERIFIED as still open | Copy-only, subjective; no code claim to verify — correctly left open |
| §3.2 `standards.trust` visual legibility | Likely already closed, not confirmed line-by-line | UNVERIFIED-NEEDS MANUAL CHECK (partially corroborated) | `trust` field referenced in `RankProgress.vue`, `RanksPage.vue`, `OverviewPage.vue`, `WorkoutPage.vue`, `ExerciseInfoPanel.vue`, `InfoToggle.vue` confirmed via grep — doc's own claim of field-reference presence holds, but visual-rendering distinction not independently confirmed this pass either |
| §4 RPE/notes surfacing | Resolved and shipped 2026-09-04 | VERIFIED COMPLETE | `RpeCapture.vue` and `NoteCapture.vue` exist under `packages/client/src/components/workout/`, referenced in `WorkoutPage.vue` |
| §5 Overall Lifter Rank promotion | Likely already closed | VERIFIED COMPLETE (presence, not prominence) | `overallRank`/`OverallRank` referenced in `OverviewPage.vue`, `RanksPage.vue`, `WorkoutPage.vue`, `WorkoutDetail.vue` per grep — matches doc's own caveat that "prominence" itself is a taste call, not re-verified visually |
| §5a Overview/Ranks/Profile duplication cut | ✅ closed 2026-09-03 | VERIFIED COMPLETE | `ProfilePage.vue` has only one "Daten-Export" heading (:432); `OverviewPage.vue` has none; `rankCopy.ts` exports `LP_EXPLAINER`, consumed by `OverviewPage.vue` and `RanksPage.vue` |
| §5a Profile domain grouping (4 headers) | ✅ closed | VERIFIED COMPLETE | `ProfilePage.vue:239` Trainingsprofil, `:361` Fortschritt, `:378` "Daten & Server", `:440` Über — all `class="group-header"`; `.card--quiet` applied at :380,388,419,431 |
| §5a Overview priority surfacing (`.tile--priority`) | ✅ closed | VERIFIED COMPLETE | `OverviewPage.vue:233` ErholungszoneCard has `class="tile--priority"`, `:236` launchpad section also has it |
| Track R Foundation: `TruncatingLabel`, `ThumbZoneAction`, `DensityScope`/`useDensityMode`, consolidated touch-target token | Shipped | VERIFIED COMPLETE | `packages/client/src/components/ui/TruncatingLabel.vue`, `ThumbZoneAction.vue`, `DensityScope.vue`, `packages/client/src/composables/useDensity.ts` all exist; `tokens.css:227` `--touch-target-min` |
| Track R Workstream B: `rank_events` plausibility nullable column (schema migration) | Shipped | VERIFIED COMPLETE | `packages/db/src/schema.ts:286` `plausibilityReason: text(...)` enum column with citing comment, `:147` `plausibilityMultiplier` |
| Track R Workstream B: sibling plausibility bug in `WorkoutPage.vue` recap panel | Fixed | VERIFIED COMPLETE | `WorkoutPage.vue:129` `sessionRankUps.value.filter((r) => !r.plausibilityNote)` |
| Track R Workstream C: drag-reorder on routine list (`useDragReorder`) | Shipped, with fill-mode fix | VERIFIED COMPLETE | `RoutineList.vue:20,54,73,273,286` — `useDragReorder` imported/used, comments referencing the fill-mode fix |
| Track R Workstream C: unmangled-umlaut slug generation | Fixed | VERIFIED COMPLETE | `AddCustomExerciseForm.vue:68` `.replace(/ü/gi, (m) => (m === "Ü" ? "Ue" : "ue"))` |
| Track R Workstream E: duplicate-import-button fix (Runs) | Fixed | VERIFIED COMPLETE | `RunsPage.vue:130,153` — single `triggerImport` button with comment explaining which button was kept |
| Full test suite / typecheck / lint clean (Phase 0 and Track R closure claims) | Passing | VERIFIED (typecheck only, this pass) | `pnpm -w run typecheck` ran clean across all 5 packages (shared, db, client, server, ingest) — full vitest suite not re-run this pass due to time budget |
| Item 13 (open question): live device/browser verification pass still owed | Still open | VERIFIED as still open (no reason to doubt) | No dev-server/browser artifacts found in repo suggesting this was done; consistent with doc's own admission |

## Verified Completed Items
* **§1.2 Silent routine-save failure**: `packages/client/src/components/routine-wizard/RoutineWizard.vue` lines 350, 361, 366 — reps are rounded/clamped, the save call is wrapped in try/catch, and a toast fires on failure. All three claimed fix parts are present.
* **§1.3 NumberStepper select-on-focus**: `packages/client/src/components/ui/NumberStepper.vue:116` — `.select()` called on focus, exactly as claimed.
* **§1.4/§1.5 Top-HUD dedup**: `packages/client/src/App.vue` has a `hideTopHud` computed (line 135) with a comment explicitly citing this exact finding, gating the `.top-hud` block.
* **§1.6 Muscle-diagram fix**: `packages/client/src/components/ui/MuscleFigure.vue` lines 126/140 use `aspect-ratio: 200 / 369` and `object-fit: contain`, matching the described corrected root cause.
* **§1.7 Touch-target floor**: `packages/client/src/styles/tokens.css` consolidates the 44px floor into a single `--touch-target-min` token (line 227) used across `.btn-close`, `.btn-primary`, `.btn-secondary`, and `NumberStepper` — an even cleaner implementation than the doc's literal-44px description implies (later superseded by the Foundation task's tokenization, noted in the token's own comment).
* **§1.8 Segmented-control neutral color**: `packages/client/src/components/ui/WorkoutRunsSwitcher.vue:61` uses a fixed `var(--blue)` regardless of section, with a comment explaining the prior per-section behavior it replaced.
* **§1.9a Token visibility toggle**: Both `ProfilePage.vue` and `AuthGate.vue` implement independent `tokenVisible` refs with 👁/🙈 buttons, matching the "both surfaces" claim (lens-3 originally only caught the Profile instance).
* **§1.9 Wizard step relabel**: `RoutineWizard.vue:440-443` only shows "3 Fertig" when `!showFastPath`, exactly the relabel-not-reroute fix described, with an explicit citing comment.
* **§1.10 Bodyweight empty-state copy**: `OverviewPage.vue:347-350` now names the concrete action ("Trag dein Körpergewicht in Profil ein").
* **§4 RPE/notes**: `RpeCapture.vue` and `NoteCapture.vue` both exist and are wired into `WorkoutPage.vue`, matching the claimed shipped state.
* **§5a duplication cut + grouping + priority tiles**: All three sub-claims (single Daten-Export entry point, unified `LP_EXPLAINER`, four Profile group headers, `.tile--priority` on both ErholungszoneCard and launchpad) verified directly in source.
* **Track R Foundation primitives**: `TruncatingLabel.vue`, `ThumbZoneAction.vue`, `DensityScope.vue`, `useDensity.ts` all exist as claimed, none were phantom files.
* **Track R plausibility-column migration**: `packages/db/src/schema.ts` has the claimed nullable `plausibilityReason` enum column with a comment explaining exactly the design tension (PEAK_ELIGIBILITY_FLOOR vs. plausibility.ts's own floor) the workplan describes.
* **Track R umlaut slug fix**: `AddCustomExerciseForm.vue:68` converts `ü`/`Ü` to `ue`/`Ue` before slugifying, preventing the described permanent-corruption bug.
* **Typecheck**: `pnpm -w run typecheck` (via `pnpm -r --filter=./packages/* run typecheck`) completes with no errors across `shared`, `db`, `client` (vue-tsc), `server`, and `ingest` — consistent with the document's repeated "typecheck clean" claims, though this only re-confirms the current state, not the historical claim that it was clean at each intermediate commit.

## Unresolved & False Positives
* **§2 Personal Records ledger — FALSE POSITIVE (stale, inverted direction).** The document states, under its own "Status check 2026-09-03" note: *"no `GET /api/prs`-shaped route exists in `packages/server/src/routes`, and no Personal Records screen/component exists in the client — this phase has not been started"* and marks the whole section ⏳ **not started**, calling it "the top of the remaining work queue." This is now **false**: the feature is fully implemented —
  - `packages/server/src/routes/prs.ts` defines `GET /api/prs` (registered in `packages/server/src/app.ts:19`), backed by `packages/server/src/services/prService.ts`.
  - `packages/client/src/stores/prStore.ts` and `packages/client/src/services/prService.ts` exist client-side.
  - `packages/client/src/pages/RecordsPage.vue` exists and is routed at `/records` (`packages/client/src/router.ts:14`).
  - It is reachable in the UI: `RanksPage.vue:70` has a `<router-link to="/records">`.
  - **One partial gap remains relative to the doc's own spec**: the doc says "the existing finish-sequence PR beat should link into this ledger" — `FinishSequence.vue` was grepped for `record`/`/records`/`prStore` and has **no** such link; the only entry point found is from `RanksPage.vue`, not the finish sequence. So the *feature* is done, but the specific finish-sequence integration point the doc calls for is not (or wasn't found).
  This is not something the reviewing pass caused — the "not started" claim in the document appears to simply be out of date relative to current `master`, likely because the PR ledger was implemented as part of (or alongside) the 2026-09-04 Track R Wave 1 merge without the workplan's §2 status line being updated afterward. This is the single most significant discrepancy found: a document section explicitly flagged as "the top of the remaining work queue" is actually already shipped.
* **§1.10 Bottom-anchor CTAs**: not independently verifiable through static grep (a viewport/thumb-reach layout claim); no explicit "done" marker in the document for this specific bullet either, so it is correctly treated as still-open by the document itself — no discrepancy, just unverifiable via this method.

## Unverified (Needs Manual Check)
* **§3.2 `standards.trust` visual legibility**: Confirmed via grep that the `trust` field is referenced across six `.vue` files (`RankProgress.vue`, `RanksPage.vue`, `OverviewPage.vue`, `WorkoutPage.vue`, `ExerciseInfoPanel.vue`, `InfoToggle.vue`), matching the document's own grep-based evidence. Whether each of these renders a genuine *visual* distinction (not just reads the field) requires a live/rendered check the document itself also flags as unconfirmed — status carried forward as-is, no new evidence either way this pass.
* **§5 Overall Lifter Rank prominence**: Field/component presence confirmed across four screens, matching the document's claim, but "prominence" (a design/taste judgment, not a boolean) is not something static code reading can settle — consistent with the document's own framing.
* **§1.10 Bottom-anchor primary CTAs (thumb reach)**: layout/viewport-dependent, not checked via static analysis.
* **Item 13 — live device/browser verification pass**: the document says this is still owed and open. No evidence was found either confirming or refuting this (no screenshots, no dev-server logs, no CI artifacts in the repo checked). Treated as consistent with the document's own "still open" status.
* **Full vitest test suite**: not re-run this pass due to time budget — only `typecheck` was executed as a quick corroborating signal. The document's repeated claims of "full test suite" passing at various checkpoints were not independently re-run.
